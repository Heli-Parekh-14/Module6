Create a new project in visual studio -> ASP.NET Web application -> Web API.
Change the code for Global.asax.cs as per file uploaded here.
Change the code for WebApiConfig.cs in App_Start folder as per file uploaded here.
Add a class named Employee.cs in Models folder. Put the code into newly created model.
Add a controller named EmployeeController in Controllers folder and put the code accordingly.

**Postman Desktop Agent is used for sending requests to the API.**
